import numpy
from numpy import array
x=array[0,1,1]
w=array[1,1,1]
fx=w@x
print(f'input vector:{x}\n weighted vector:{w}')
print(f'weighted sum:{fx}')

def linear_threshold_gate(fx:int, T:float)->int:
	if fx>=T:
		return 1
	else:
		return 0
T=1
activation=linear_threshold_gate(fx,T)
print(f'activation (if threshold=1):{activation}')

T=5
activation=linear_threshold_gate(fx,T)
print(f'activation (if threshold=5):{activation}')




import numpy as np
features=np.array([1,1][1,-1],[-1,1],[-1,-1])
labels=np.array([-1,1,-1,-1])



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  git token


Scoring for Regular session: (Technical & Softskills) Pass mark - 70
1. Attendance -15(out of 15)
2. Switching on camera-15(out of 15)
3. e-learning topic/Assignments completion on LMS- 25(out of 25)
4. Completing mentors assignments on time -25(Out of 25)
5. Participation in the sessions (Asking questions, answering questions asked by mentor) -20(Out of 20)